# Run `grow_training_data.py predictive_means_and_vars_iter{i}.npy` appends 
# another row in the `train_idxs.npy` by picking the top 100 molecules
# (rather, inidices in the training set) with highest predictive variance.
